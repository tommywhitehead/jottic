"use client";
import { jsx } from 'react/jsx-runtime';
import { useGroupInfo } from '@liveblocks/react';
import { cn } from '../../utils/cn.js';


function GroupDescription({
  groupId,
  className,
  ...props
}) {
  const { info } = useGroupInfo(groupId);
  return info?.description ? /* @__PURE__ */ jsx("span", {
    className: cn("lb-group-description", className),
    ...props,
    children: info.description
  }) : null;
}

export { GroupDescription };
//# sourceMappingURL=GroupDescription.js.map
